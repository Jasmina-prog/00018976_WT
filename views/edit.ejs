<!DOCTYPE html>
<html>
<head>
    <title>Edit Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../styles/style.css">

    <style>
        body{
            background-size: cover;
            background-repeat: no-repeat;
            background-image: url('/pexels-pixabay-235985.jpg');
            color: rgb(255, 197, 5) !important;
            font-family: 'Press Start 2P', cursive !important;
            font-weight: 700 !important;
        }
        .custom-focus:focus {
            border: none !important; 
            outline: none !important; 
        }
        .button{
            font-family: 'Cormorant Garamond', serif;
            background-color: #d4b483 !important;
            color: #4b2e1f !important;
            padding: 10px 20px !important;
            font-size: 1rem !important;
            border-radius: 8px !important;
            transition: background-color 0.3s, box-shadow 0.3s !important;
            box-shadow: 2px 2px 5px #4b2e1f !important;
        }
    </style>

</head>
<body class="bg-light">
    <%- include('partials/navbar') %> 
    <div class="container mt-5">
        <h1 class="mb-4">Edit Book</h1>

        <!-- form for editing a book -->
        <form action="/books/<%= book._id %>?_method=PUT" method="POST">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input type="text" id="title" name="title" value="<%= book.title %>" class="form-control bg-transparent border-dark custom-focus text-white fw-bold fs-4" required>
            </div>
            <div class="mb-3">
                <label for="author" class="form-label">Author</label>
                <input type="text" id="author" name="author" value="<%= book.author %>" class="form-control bg-transparent border-dark custom-focus text-white fw-bold fs-4" required>
            </div>
            <div class="mb-3">
                <label for="review" class="form-label">Review</label>
                <textarea id="review" name="review" class="form-control bg-transparent border-dark custom-focus text-white fw-bold fs-4" rows="4" required><%= book.review %></textarea>
            </div>
            <div class="d-flex gap-3">
                <button type="submit" class="btn button">Update Book</button>
                <a href="/books" class="btn button">Back</a>
            </div>
        </form>
    </div>
</body>
</html>